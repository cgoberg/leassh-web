# leassh-web tracker

Canonical task truth for `cgoberg/leassh-web`, the Vercel source of https://leassh.com.
The Forge venture dispatcher reads `origin/main` via git transport. Do not treat the dirty live checkout at `/opt/leassh-web` as tracker truth.
